services:
  mujian:
    image: ghcr.io/felix2yu/mujian:latest
    ports:
      - "8080:8080"
    volumes:
      - mujian-data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

volumes:
  mujian-data:
